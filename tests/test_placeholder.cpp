#include <catch2/catch_test_macros.hpp>

// Placeholder so the test target has a source and CTest has a case to run.
// Delete this once CppBundler has real tests.
TEST_CASE("placeholder", "[placeholder]") {
    REQUIRE(true);
}
{
	"version": "0.2.0",
	"configurations": [
		{
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopOnEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "${defaultBuildTask}",
            "initCommands": []
        }
	]
}
